import React from 'react';
import ReactDOM from 'react-dom';

import Index from 'Index';

import textStyle from 'style/text.css'; //eslint-disable-line no-unused-vars
import normalizeStyle from 'style/react-toolbox-normalize.scss'; //eslint-disable-line no-unused-vars

ReactDOM.render(
	<Index />,
	document.getElementById('root')
);
